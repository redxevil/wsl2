apiVersion: v1
kind: Service
metadata:
  name: emailservice
  namespace: dev-environment
spec:
  selector:
    app: emailservice
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
