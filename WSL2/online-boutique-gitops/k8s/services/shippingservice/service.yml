apiVersion: v1
kind: Service
metadata:
  name: shippingservice
  namespace: dev-environment
spec:
  selector:
    app: shippingservice
  ports:
    - protocol: TCP
      port: 50051
      targetPort: 50051
  type: ClusterIP
